[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: uzun süren testler (performans)
    integration: birden fazla modülü kapsayan entegrasyon testleri
